{
  "name": "mcm-code-browser",
  "private": true,
  "type": "module",
  "scripts": {
    "build:manifest": "node tools/build-manifest.mjs",
    "dev": "npm run build:manifest && vite",
    "build": "npm run build:manifest && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
